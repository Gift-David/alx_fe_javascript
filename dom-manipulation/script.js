const newQuote = document.getElementById('newQuote')
const quoteDisplay = document.getElementById('quoteDisplay')

const quote = []

function showRandomQuote(params) {
    
}

function createAddQuoteForm(params) {
    
}

// ["text", "category"]
// ["random", "innerHTML"]
// ["Math.random"]

showRandomQuote()
createAddQuoteForm()
